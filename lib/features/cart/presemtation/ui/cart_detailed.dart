import 'package:flutter/material.dart';

class CartDetailed extends StatelessWidget {
  const CartDetailed({super.key});

  @override
  Widget build(BuildContext context) {
    return const Material();
  }
}
